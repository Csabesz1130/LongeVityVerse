import React from 'react';
import ClientLayout from "@/components/LayoutClient";

const Hub = () => {
  return (
    <ClientLayout>
      <main>
        <h1>Test Hub</h1>
        <p>If this renders, start adding other elements back in one at a time.</p>
      </main>
    </ClientLayout>
  );
};

export default Hub;
